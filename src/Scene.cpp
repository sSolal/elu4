#include "Scene.h"

// ObjectInstance is a plain aggregate (see Scene.h); projection lives in Level2.cpp
// (projectObjectInstance), shared by every draw path. This translation unit is kept
// so the build's source list is stable.
